name: Secret_scan

on:
  push:
  pull_request:


jobs:
  Check:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Gitleaks Scan
      uses: gitleaks/gitleaks-action@v2